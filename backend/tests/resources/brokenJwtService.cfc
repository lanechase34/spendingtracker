component {

    function parseToken(
        string token,
        boolean storeInContext,
        boolean authenticate
    ) {
        throw(type = 'SomeUnexpectedException', message = 'Simulated infrastructure failure');
    }

}
