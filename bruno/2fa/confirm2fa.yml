info:
  name: confirm2fa
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/v1/me/2fa/confirm?code=123123"
  headers:
    - name: x-auth-token
      value: "{{access_token}}"
    - name: x-csrf-token
      value: "{{csrf_token}}"
  params:
    - name: code
      value: "123123"
      type: query
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
