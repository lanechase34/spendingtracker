info:
  name: readLog
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/admin/logs/server.out.txt"
  headers:
    - name: x-auth-token
      value: "{{access_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
