info:
  name: viewLogs
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/v1/admin/logs"
  headers:
    - name: x-auth-token
      value: "{{access_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
