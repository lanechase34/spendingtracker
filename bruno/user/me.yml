info:
  name: me
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/v1/me"
  headers:
    - name: x-auth-token
      value: "{{access_token}}"
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
