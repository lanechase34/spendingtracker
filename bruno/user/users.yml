info:
  name: users
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/api/v1/users"
  headers:
    - name: x-auth-token
      value: "{{access_token}}"
  body:
    type: multipart-form
    data:
      - name: records
        type: text
        value: "10"
      - name: page
        type: text
        value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
